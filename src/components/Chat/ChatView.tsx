// import React from 'react';
// import useStore from '@/store/slices';
// // import { cn } from '@/utils/helpers';
// // import Header from './Header/Header';
// import ViewComponent from '../Sidebar/ViewController';

// type ChatViewProps = {};

// const ChatView: React.FC<ChatViewProps> = () => {
//   //   const isChatOpen = useStore((state) => state.chatbar.isChatOpen);

//   // const chatView = useStore((state) => state.chatbar.chatView);

//   if (chatView === 'close') return null;

//   return <aside>{ViewComponent[chatView].component}</aside>;
// };
// export default React.memo(ChatView);
